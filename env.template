# Sliver C2 Training Environment Configuration
# Copy this file to .env and fill in your values
# cp env.template .env
# Then edit .env with your actual values

# ============================================================================
# Domain/Hostname Configuration
# ============================================================================
# Your domain name or IP address (used for SSL certificates and C2 URLs)
# Examples: 
#   - Domain: example.com
#   - IP: 192.168.1.100
#   - IP with port binding: 0.0.0.0
DOMAIN=your-domain-or-ip.com

# ============================================================================
# SSL Certificate Configuration
# ============================================================================
# SSL certificate file name (without path)
# The actual files should be placed in: nginx/certs/
SSL_CERT_NAME=${DOMAIN}.crt
SSL_KEY_NAME=${DOMAIN}.key

# ============================================================================
# Sliver C2 Configuration
# ============================================================================
# Sliver HTTP listener port (internal to container, exposed via nginx)
SLIVER_HTTP_PORT=8080

# Auto-start HTTP listener (1 = enabled, 0 = disabled)
SLIVER_AUTOSTART_HTTP=1

# Sliver C2 endpoint path (used in implant generation)
# This is the path that nginx will proxy to Sliver
SLIVER_C2_PATH=/api/current/

# Sliver C2 full URL (used for implant generation)
# Format: https://${DOMAIN}:443${SLIVER_C2_PATH}
# This will be automatically constructed, but you can override if needed
SLIVER_C2_URL=https://${DOMAIN}:443${SLIVER_C2_PATH}

# ============================================================================
# Port Configuration
# ============================================================================
# External ports exposed on host
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443

# ============================================================================
# Network Configuration
# ============================================================================
# Docker network name (internal)
NETWORK_NAME=demo_net

# ============================================================================
# Volume Paths
# ============================================================================
# Path to Sliver configuration directory (on host)
# Default: ${HOME}/.sliver
SLIVER_CONFIG_DIR=${HOME}/.sliver

# Path to builds directory (relative to project root)
BUILDS_DIR=./builds

